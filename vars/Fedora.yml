---
# The system packages to install.
package_names:
  - cargo
  - gettext-envsubst
  - make
  - openssl-devel
  - openssl-devel-engine
  - rpm-build

# The version of amazon-efs-utils to install.
version: 2.3.1
